#Expone Redis a otros pods en el cluster

apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: restaurante
spec:
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
