#Expone Keycloak al exterior con Load Balancer

apiVersion: v1
kind: Service
metadata:
  name: keycloak-lb
  namespace: restaurante
spec:
  type: LoadBalancer
  selector:
    app: keycloak
  ports:
    - name: http
      port: 8001
      targetPort: 8080

